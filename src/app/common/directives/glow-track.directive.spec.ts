import { GlowTrackDirective } from './glow-track.directive';

describe('GlowTrackDirective', () => {
  it('should create an instance', () => {
    const directive = new GlowTrackDirective();
    expect(directive).toBeTruthy();
  });
});
